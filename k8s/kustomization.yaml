apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ingress.yaml is intentionally not listed here - it needs a real hostname
# first. Apply it separately once you've edited it: kubectl apply -f ingress.yaml
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
